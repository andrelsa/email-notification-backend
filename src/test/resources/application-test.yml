spring:
  datasource:
    url: jdbc:tc:postgresql:16-alpine:///email_notification
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

management:
  endpoint:
    health:
      show-details: always

